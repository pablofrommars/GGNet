# q-audit — Implementation Plan

Living artefact for executing the reconciled audit chain (`audit.md` rev round-2 ← `audit-challenge.md` ← `audit-challenge-codex.md`). Agent-generated; no user-maintained sections yet.

## Context for a fresh session

Read in this order before touching code:

1. This file end-to-end (contract, gates, invariants, step rows).
2. `plan/q-audit/audit-challenge.md` — the **Survivors** section holds each finding's verified mechanism with `file:line` evidence; the **Counter-review reconciliation** section holds the MF-2/MF-6/MF-10 refinements the steps depend on.
3. `plan/q-audit/audit.md` — canonical finding statements (incl. #11) and suggested-fix framing; where it conflicts with a resolved decision gate below, the gate wins.

The repo's `CLAUDE.md` + `.github/instructions/*.md` load automatically and govern style, unions, goldens, and testing conventions. Read every file before changing it — the audit evidence is line-accurate as of 2026-07-28 but the tree wins.

## Execution contract

- **Collaborative cadence**: implement exactly one step, run the three gates, update the step row + acceptance matrix in this file with concrete evidence (test names, golden diffs eyeballed, command outcomes), report, then STOP. "go"/"next" releases one step. "Implement the remaining steps" flips the persisted cadence to autonomous — it does not expand scope or authority.
- Reconcile each step against the current tree before starting; preserve any human edits; log material deltas in the table below and re-evaluate affected pending steps.
- STOP for: failed verification beyond recovery, golden churn where a step forbids it (step 8), scope/blast-radius growth beyond the step row, missing authority, or any decision this file doesn't already resolve.
- **Failure recovery**: classify {introduced / pre-existing / environmental / flaky / unknown} before changing code; ≤ 2 in-scope remediation cycles per distinct failure; STOP if unknown, repeating, or out-of-scope.
- Completion = every acceptance criterion passed or explicitly user-waived; never self-waive, never weaken a test to pass.

### Gate commands

```
# build (sandbox-safe)
DOTNET_CLI_DO_NOT_USE_MSBUILD_SERVER=1 dotnet build GGNet.slnx -warnaserror -m:1 -nodeReuse:false
# tests (requires sandbox-off — vstest IPC is blocked in-sandbox)
dotnet test GGNet.slnx
# format
dotnet format whitespace GGNet.slnx --verify-no-changes && dotnet format style GGNet.slnx --verify-no-changes
```

## Outcome

All canonical must-fix findings 1–11 and should-fix 1 resolved in `src/GGNet` with regression tests, all three gates green after every step, and every golden re-pin eyeballed and recorded. Verdict target: ship-ready on these findings.

**Status 2026-07-28: complete.** All 11 steps done, C1–C12 passed, none waived. Tests 290 → **361** passed (3 skipped, 0 failed); build 0 warnings / 0 errors; format clean. 8 goldens re-pinned (5 in step 10, 3 in step 11), each eyeballed and recorded in its step row; steps 1–9 churned nothing. The 11 audit steps themselves moved no public surface. The staged set is **not** surface-neutral, however: separately authorized follow-on work removed `SparkLine`, `SparkLineTooltip` and `PlotBase` from `PublicApiTests.GGNet` — a deliberate break taken inside the open 2.0 window, recorded in the material-delta log. No open decisions: the one item raised outside the findings — the unguarded `Panels[0]` in `Components/SparkLine.razor.cs:44` — was closed by the user as refactor work, not a point fix (see the material-delta log).

## Non-goals

- Area/ribbon same-x dedup contract (withdrawn as a defect; optional docs note only, not scheduled).
- Hardening `Point.Shape`'s `radius <= 0` guard beyond what the scale fixes make unreachable.
- Changing `SortedBuffer` semantics (set-like dedup stays; MF-9 gets its own structure).
- Any public API surface change (`PublicApiTests` manifests must not churn) — held for steps 1–11; the later SparkLine/`PlotBase` removal is follow-on work outside this plan, not a relaxation of it.
- New skill examples / gallery scenarios beyond what a finding's verification requires.

## Invariants and ripple rules

- **Gates after every step**: `dotnet build GGNet.slnx -warnaserror` · `dotnet test GGNet.slnx` · `dotnet format whitespace|style GGNet.slnx --verify-no-changes`.
- **Goldens**: byte-compare; a re-pin is deliberate, eyeballed, and listed in the step's status. Steps are ordered so golden-churning work comes last.
- **Overload discipline**: `<param>` doc edits apply to the entire family atomically (`OverloadConsistencyTests` docs-agree). The expand re-doc touches all 13 "additives are data units" sites in `BuilderExtensions.cs` (verify none exist in other partials at step time).
- **Closed unions**: extending/altering `ScreenPolygon` ripples to the `Area.razor` walker in the same change; no discard arms (`CS8509`=error).
- **Culture**: any new emission stays invariant; `LocaleTests` + culture CI job are the check.
- **Style**: tabs, Allman, file-scoped namespaces, no `_` prefix, `internal` default, `GGNetUserException` for surface guards.
- **Environment**: sandboxed builds need `DOTNET_CLI_DO_NOT_USE_MSBUILD_SERVER=1 … -m:1 -nodeReuse:false`; `dotnet test` requires sandbox-off (MSBuild/vstest IPC sockets blocked in-sandbox).

## Ordering rationale

Zero-churn and logic-only fixes first (1–8) to keep the golden baseline stable while the bulk of behavior is fixed and tested; golden-churning steps last (9–11), each isolating its own churn for eyeballing. Step 8 (datetime) is churn-*guarded*: its acceptance requires byte-identical datetime goldens.

## Decision gates — resolved 2026-07-28

| Gate | Decision (user) |
|---|---|
| MF-2b expansion units | Re-document as transformed-scale units (13 doc sites); no code change to `SetRange` additives |
| MF-8 empty-facet state | Zero panels + guards; validate `nrows`/`ncolumns` positive with `GGNetUserException` |
| MF-10 hole mechanism | Winding normalization at compose (signed-area orientation, holes wound opposite, SVG default `nonzero`) |
| SF-1 palette policy | Throw `GGNetUserException` at surface/commit with actionable message |
| MF-4 degenerate mapping | Midpoint default (folded into plan approval — this plan) |

No gates remain open.

## Authority state

- **Cadence**: autonomous since 2026-07-28 ("go all steps unless gate decision block", mid-step-3). Steps 1–2 ran collaboratively; 3–11 autonomously. Scope and authority unchanged by the flip.
- **Skills**: none selected/required. `csharp-lsp` navigation (read-only routing) may activate implicitly; it cannot expand scope or effects.
- **Approved actions**: sandbox-off `dotnet test` runs (environmental necessity; each run still passes the permission gate). No external, paid, or destructive actions authorized.
- **Exclusions**: no golden re-pin without eyeball + listing; no test/criterion weakening; no `Directory.Build.props`/CPM/`.editorconfig` changes; no public-surface changes.

## Step rows

| id | goal | files | blast radius | verification | status |
|---|---|---|---|---|---|
| 1 | **MF-1**: HTML-encode all non-token text (unmatched spans and token group values) in `Markdown.Text` before wrapping tspans | `src/GGNet/Markdown.cs`; new/extended tests in `tests/GGNet.Headless.Tests` | multi-file+code | Hostile-input tests (`<script>`, attribute injection, `&`) render encoded; markdown tokens still styled; goldens byte-identical (verified zero-churn expectation); gates | **done** — `Markdown.Escape` (`SecurityElement.Escape`, the 5 XML predefined entities) applied to the unmatched head/tail slices and all four token group values; `using System.Security;`. New `tests/GGNet.Headless.Tests/MarkdownTests.cs` (10 cases): `EncodesUnmatchedText`, `EncodesTokenValues`, `EncodesTextAroundTokens`, `EncodesAmpersand`, `PreservesStyling` ×5 (`[Theory]`, incl. em-dash passthrough), `HostileLabelsRenderInert` (end-to-end `.Title/.SubTitle/.Caption/.XLab/.YLab` → `XDocument` has no `script` descendant). Fail-before verified: reverting only `Markdown.cs` fails all 5 hostile/encoding cases (e.g. `<tspan font-weight="bold"><img src=x onerror="alert(1)">` emitted raw), `PreservesStyling` stays green. Zero golden churn: no `*.received.*`, no `Gallery/`/`Api/` file touched (`git status` = `Markdown.cs` + new test only). Gates: build 0 warn/0 err; tests 30+223+47 = **300** passed / 3 skipped / 0 failed (was 290, +10 = the new file); `format whitespace` + `format style` clean |
| 2 | **MF-3**: reset `cachedArea` in `Ribbon.Clear`; extend render-twice coverage to unmapped ribbons | `src/GGNet/Geoms/Ribbon/Ribbon.cs`; `tests/GGNet.Headless.Tests/RenderPipelineTests.cs` | multi-file+code | New `RenderTwice…Ribbon` test fails-before/passes-after; goldens unchanged; gates | **done** — `cachedArea = null;` added to `Ribbon.Clear()` (after `areas.Clear()`), with a comment stating why: the cache held the only reference to a shape `base.Clear()` had just removed from `Layer`. Two new tests in `RenderPipelineTests`: `RenderTwiceUnmappedRibbonIdentical` (constant-fill path — the defect) and `RenderTwiceMappedRibbonIdentical` (inherited `Scale_Fill_Discrete` path — no-regression guard). Fail-before verified: reverting only `Ribbon.cs` fails the unmapped test on **both** assertions — pass 2 does not contain `fill="#23d0fc"` (ribbon gone entirely) and the two renders differ at line 44 — while the mapped test stays green. Zero golden churn: no `Gallery/`/`Api/` file touched, no `*.received.*`. Gates: build 0 warn/0 err; tests 30+225+47 = **302** passed / 3 skipped / 0 failed (+2); `format whitespace` + `format style` clean |
| 3 | **MF-4a/4b**: replace `(0,0)` sentinel with an init flag in `FillContinuous` + `SizeContinuous`; degenerate range maps to midpoint (middle color / midpoint of size range) | `src/GGNet/Scales/FillContinuous.cs`, `Size.cs`; tests | multi-file+code | Tests: leading-zero retained; constant fill renders a color; constant size returns finite mid-range radius (no NaN); gates | **done** — private `trained` flag in both scales replaces the `limits.min == 0 && limits.max == 0` sentinel in `Train`, reset in `Clear` (Size keeps its `defined` short-circuit, so explicit `limits:` still bypass training). Degenerate mapping: `FillContinuous.Map` returns `colors[(colors.Length - 1) / 2]` instead of `string.Empty` (same index a mid-range key takes under the gradient formula); `SizeContinuous.Map` returns `range.min + (range.max - range.min) / 2.0` guarded by `limits.max <= limits.min`, placed after the oob/clamp branches so out-of-range behavior is untouched. New `tests/GGNet.Headless.Tests/ContinuousAestheticScaleTests.cs` (8 cases): leading-zero retention, constant→midpoint, and `Clear`-then-retrain for each scale, plus end-to-end `ConstantFillLayerRenders` (bar layer emits `fill="#888888"`) and `ConstantSizeLayerEmitsFiniteRadii` (SVG has no `NaN`, emits `r="6"`). Fail-before verified: reverting both scale files fails **all 8**. Zero golden churn. Gates: build 0 warn/0 err; tests 30+233+47 = **310** passed / 3 skipped / 0 failed (+8); format clean |
| 4 | **MF-6**: `while (i <= end)` → `i < end` in `DayMonth` trailing loop | `src/GGNet/Scales/DiscreteDates.cs`; tests | multi-file+code | Repro test (Jan 30–Feb 2) crashes before, renders after; windowed test asserts no out-of-window tick; gates | **done** — one-character fix + a comment stating `end` is exclusive like every other loop in the method. New `tests/GGNet.Headless.Tests/DiscreteDatesTests.cs` (5 cases): `CommitAcrossAMonthBoundary` (the audited Jan 30–Feb 2 trace), `RenderAcrossAMonthBoundary` (end-to-end through the DSL), `CommitAcrossABoundaryFromStart` `[Theory]` ×2 (year boundary Dec 30→Jan 4, leap-Feb Feb 27→Mar 3), `WindowedCommitEmitsNoTickOutsideTheWindow` (Jan 30–Feb 3 windowed to Feb 2: index 4 is a valid element outside the window; asserts `Breaks` excludes 4.0). Fail-before verified: reverting `DiscreteDates.cs` fails **all 5** — the four full-range cases with `ArgumentOutOfRangeException` (confirming the challenge's correction of the audit's `IndexOutOfRangeException`), the windowed case on the stray tick. Zero golden churn (no pinned golden crossed a month boundary — the crash guaranteed it). Gates: build 0 warn/0 err; tests 30+238+47 = **315** passed / 3 skipped / 0 failed (+5); format clean |
| 5 | **MF-9**: multiplicity-preserving sample storage for boxplot percentiles (leave `SortedBuffer` untouched) | `src/GGNet/Geoms/Boxplot/Boxplot.cs` (+ small buffer/list); tests | multi-file+code | Percentile test `[1,1,1,10]` yields duplicate-aware quartiles/median/whiskers; boxplot golden unchanged unless sample data had duplicates (eyeball if churn); gates | **done** — new `src/GGNet/Buffers/SampleBuffer.cs`: SortedBuffer's shape minus the dedup (`items.Insert(index < 0 ? ~index : index, item)`), no `IndexOf`/set-mutation surface. `Boxplot` switches its per-group sample buffers and `Percentile`'s parameter to `SampleBuffer<double>`; `boxes` stays a `SortedBuffer` (keyed by coordinate — one box per coord is the correct set semantics). `SortedBuffer` untouched. Tests: `SampleBufferTests` (5 — duplicates kept, out-of-order/descending sort, compare-equal custom comparer, clear-and-reuse) and `BoxplotTests` (7 — `PercentilesPreserveMultiplicity` `[Theory]` ×5 pinning p10/p25/p50/p75/p90 = 1/1/1/3.25/7.3 over `[1,1,1,10]`, `PercentilesOverDistinctSamplesAreUnchanged`, end-to-end `DuplicateObservationsChangeTheRenderedBox`). Fail-before verified by reinstating dedup in `SampleBuffer.Add`: **10 of 12 fail**; the 2 survivors are the duplicate-free controls. Checked `Geoms/Violin` for the same defect — it buffers `Stat.Density` grid points, not raw samples, so it is not affected (no scope growth). Zero golden churn: the gallery's `grouped` fixture has no repeated value within a group, so `GalleryTests.Boxplot` is byte-identical. Gates: build 0 warn/0 err; tests 30+250+47 = **327** passed / 3 skipped / 0 failed (+12); format clean |
| 6 | **MF-8**: skip faceted legend path when `Panels` empty; validate `nrows`/`ncolumns` positive at the `Facet_Wrap` surface (`GGNetUserException`) | `src/GGNet/PlotContext.cs`, facet builder entry; tests | multi-file+code | Empty-faceted-source test renders well-formed empty SVG without crash; invalid dims throw; gates | **done** — `BuildLegends`' faceted branch is now `else if (Panels.Count > 0)` (zero panels = empty plot, per the decision gate; no synthetic panel); `Facet_Wrap` guards `nrows is <= 0` / `ncolumns is <= 0` with `GGNetUserException` (added `using Exceptions;` to `BuilderExtensions.cs`). Audited every other `Panels[…]` access: all are loop-bounded by `Panels.Count` except `Components/SparkLine.razor.cs:44`, which is out of this row's blast radius — see the material-delta log. New `tests/GGNet.Headless.Tests/EmptyFacetTests.cs` (8 cases): empty faceted render (well-formed SVG, no marks), empty + legend (the crashing path), empty→refilled streaming round-trip, `NonPositiveFacetDimensionsThrow` `[Theory]` ×4, positive dims accepted. Fail-before verified: **7 of 8 fail** — the three render cases with `ArgumentOutOfRangeException` from `Panels[0]`, all four dimension cases with no exception thrown. Zero golden churn. Gates: build 0 warn/0 err; tests 30+258+47 = **335** passed / 3 skipped / 0 failed (+8); format clean |
| 7 | **SF-1**: palette validation — exhaustion and empty palettes throw `GGNetUserException` with actionable message (entry point where knowable, commit where cardinality-dependent) | `src/GGNet/Palettes/Utils.cs`, `Discrete.cs`, `src/GGNet/Scales/FillContinuous.cs` (+ entry guards); tests | multi-file+code | Tests: categories > palette throws with message; empty continuous palette throws at surface, not `IndexOutOfRangeException` mid-render; gates | **done** — new `Palettes.Utils.NonEmpty(palette, scale)` is the shared empty-palette guard, applied in the `Palettes.Discrete` field initializer (the single choke point every `Scale_*_Discrete` flows through, whether the caller passes `string[]` or a prebuilt `Palettes.Discrete`) and in `FillContinuous`'s. Exhaustion throws from `Discrete.Set()` — the only place the key count is known — via `Utils.Sample(…) ?? throw`, with a message carrying both counts and the required minimum. Behavior-only: no signature moved, `PublicApiTests` manifests unchanged. New `tests/GGNet.Headless.Tests/PaletteValidationTests.cs` (5 cases): exhaustion throws with both counts in the message, a sufficient palette still renders, empty discrete/continuous palettes throw at the DSL surface, and a bare `new Palettes.Discrete<string, string>([])` throws before any render. Fail-before verified: **4 of 5 fail** with "no exception was thrown" (the survivor is the sufficient-palette control). Zero golden churn. Gates: build 0 warn/0 err; tests 30+263+47 = **340** passed / 3 skipped / 0 failed (+5); format clean |
| 8 | **MF-7**: `DateTimePosition` retains every observed key at `Train`; minute sampling derived at `Commit` from sorted extent; delete dead `values.Add(date)` (`:165`) | `src/GGNet/Scales/DateTimePosition.cs`; tests | multi-file+code | Out-of-order same-day test: all points render; order-permutation test: identical output; **datetime goldens byte-identical** (churn here = design error → STOP); gates | **done** — `Train` is now `values.Add(key)` plus the first/last/min/max updates; the forward-only minute interpolation moved to a private `Sample()` called at the top of `Commit`, which walks the sorted buffer and lays a 1-minute grid per observed day from that day's first to its last key, collecting into a list before adding (never mutating the buffer mid-walk). Dead `values.Add(date)` at the old `:165` deleted. Byte-identical for in-order minute-aligned input — the old per-day grid is exactly `[dayMin … dayMax]` step 1 min — and a strict superset otherwise (observed keys off the grid are now retained instead of mapping to NaN). New `tests/GGNet.Headless.Tests/DateTimePositionTests.cs` (6 cases): `OutOfOrderSameDayKeysAreRetained`, `PermutingTrainOrderYieldsIdenticalMappings` (3 permutations), `InOrderSamplingIsUnchanged` (pins 0/30/60 — the old dense-grid indices), `EachDayIsSampledFromItsOwnExtent` (cross-midnight, pins 0/15/16/46), `KeysOffTheMinuteGridAreRetained`, end-to-end `OutOfOrderSourceRendersEveryPoint`. Fail-before verified: **5 of 6 fail**; the survivor is `InOrderSamplingIsUnchanged` — the no-churn guarantee proven by test, not by inspection. **Golden churn: none.** No gallery scenario uses `LocalDateTime`, so there is no datetime golden to re-pin, and no `Gallery/`/`Api/` file changed. Gates: build 0 warn/0 err; tests 30+269+47 = **346** passed / 3 skipped / 0 failed (+6); format clean |
| 9 | **MF-2a+2b**: transform explicit `Limits` endpoints at commit in `Extended`/`Log10`; re-document the 13 expand `<param>` sites as transformed-scale units | `src/GGNet/Scales/Extended.cs`, `Log10.cs` (+ shared helper if cleaner); `src/GGNet/BuilderExtensions.cs` docs | multi-file+code | Test: `Scale_X_Log10(limits:(1,1000))` → committed range spans log-space ≈(0,3); `XLim` on transformed scale honored in data units; `OverloadConsistencyTests` green; check `skills/ggnet/reference/scales.md` for quoted doc text (update if drifted); enumerate + eyeball any transformed-scale goldens; gates | **done** — **2a**: one shared conversion point, `protected double Endpoint(TKey? limit, double? trained)` on `Position<TKey>` beside `SetRange`, routing an explicit limit through the scale's own `Map` (the same call the geoms train through) and falling back to the trained bound; `Extended.Commit` and `Log10.Commit` both now call `SetRange(Endpoint(Limits.min, _min), Endpoint(Limits.max, _max))`. Identity scales are unaffected by construction (`Map` = `Identity.Apply`), so `XLim`/`YLim` on a plain continuous scale keep their exact behavior. **2b**: all **13** `expand` `<param>` sites re-documented atomically to "additives are in the scale's own space (data units on an untransformed scale, log10 exponents on a log scale, category indices on a discrete one)" — verified all 13 live in `BuilderExtensions.cs` and none in the other partials; no `SetRange` code change, per the decision gate. `skills/ggnet/reference/scales.md` checked: it lists signatures only and makes no claim about expand units — no drift, no edit needed. New `tests/GGNet.Headless.Tests/TransformedLimitsTests.cs` (6 cases): log10 limits commit to (−0.15, 3.15), sqrt limits to (−0.5, 10.5), untransformed limits unchanged at (4.75, 10.25), limited data spans the axis (`Coord` 0.045→0.955, previously the bottom thousandth), and two end-to-end renders asserting sane break labels (`>1e3<` present, the old `1e171` absent) for both `Scale_X_Log10(limits:)` and `XLim` on a log scale. Fail-before verified: **5 of 6 fail**; the survivor is the untransformed control. `OverloadConsistencyTests` + `SkillDocDriftEvals` green. Golden churn: **none** — enumerated the gallery, no scenario uses `Log10`/`Sqrt`/`XLim`/`YLim`/`limits:`, so there is no transformed-scale golden to eyeball. Gates: build 0 warn/0 err; tests 30+275+47 = **352** passed / 3 skipped / 0 failed (+6); format clean |
| 10 | **MF-5 + MF-11**: `Stack` gets positive/negative accumulators, emits non-negative heights, trains y over both endpoints and x over the drawn `x ± delta/2` | `src/GGNet/Geoms/Bar/Bar.cs`; tests | multi-file+code | Mixed-sign stack test: all segments present, non-negative screen heights, trained y covers extremes; trained x equals drawn extent; stacked-bar goldens re-pinned after eyeball (churn expected and listed); gates | **done** — `Stack()` now carries `positive`/`negative` accumulators: a non-negative segment is emitted from `positive` upward, a negative one from the decremented `negative` with height `-value`, so every rectangle has a non-negative height; y trains `Shape(Min(0, negative), Max(0, positive))` (identical to the old `Shape(0, sum)` for all-positive data) and x trains the drawn `x ± delta/2`. New `tests/GGNet.Headless.Tests/StackedBarTests.cs` (4 cases): mixed-sign in/out flows emit all 6 segments with non-negative heights and positive widths, stay inside the panel on both axes, an all-positive control, and `StackedExtentEqualsTheDrawnWidth` pinning a single bar at `1/1.1` of the panel width (was `0.8/1.76`). Fail-before: **3 of 4 fail**; the all-positive control passes both ways. **Golden churn: 5 re-pinned after eyeballing** — `GalleryTests.Bar`, `BarStacked`, `BarFlipped`, `Histogram`, `HistogramFaceted`. Diffs verified to be MF-11 only: bar heights and y positions byte-identical (e.g. `Bar` keeps 240.69/421.21/336.97), widths grow 99.59 → 117.88 (+18.4%), first bar starts at x=46.09 instead of 95.89, break/label positions follow the tightened x range, legend swatches untouched. `BarFlipped` grows on its category axis instead (49.8 → 55.3 thickness), as expected from the transpose. No negative heights and no `NaN` anywhere in the re-pinned output. Gates: build 0 warn/0 err; tests 30+279+47 = **356** passed / 3 skipped / 0 failed (+4); format clean; 0 `*.received.*` left |
| 11 | **MF-10**: winding normalization in `ComposeMultiPolygon` — signed-area orientation per ring, `Hole` rings wound opposite to exteriors, SVG default `nonzero` kept | `src/GGNet/Scene/ShapeComposer.cs` (+ `Shapes`/`Geospacial` plumbing to carry `Hole` to compose); tests + one new hole-bearing golden | multi-file+code | New golden with `Hole=true` inner ring renders hole regardless of source winding; overlapping-exteriors test stays filled; existing map goldens eyeballed if normalization reorients source data; gates | **done** — **no plumbing was needed**: `Shapes.MultiPolygon.Polygons`/`Shapes.Polygon.Path` already carry `Geospacial.Polygon`, so `Hole` was reachable in `AppendPolygon` all along, just never read. Added `SignedArea(coord, poly)` (shoelace over the projected ring; only the sign is used) and `AppendPolygon` now emits vertices forward or reversed so exteriors come out positive and `Hole` rings negative — applied in one place, so `ComposePolygon` and `ComposeMultiPolygon` both benefit. Default `nonzero` kept; no `fill-rule` emitted, per the counter-review's caveat that a blanket `evenodd` would cancel overlapping exteriors. New `tests/GGNet.Headless.Tests/PolygonHoleTests.cs` (4 cases): hole wound against its exterior (signed areas of opposite sign), all four source-winding combinations compose to the identical path, overlapping exteriors keep the same sign, and no `fill-rule` is emitted. Fail-before: **3 of 4 fail** (the survivor is the no-`fill-rule` invariant, true before and after). **Golden churn: 3** — new `GalleryTests.MapWithHole` (exterior and same-wound inner ring; composed output verified by hand to have positive exterior / negative hole), plus `GalleryTests.Map` and `BubbleMap` re-pinned after eyeballing: both diffs are a pure vertex-order reversal of the same points (identical coordinates, identical rendered fill), because both fixtures happened to be wound negative. Gates: build 0 warn/0 err; tests 30+284+47 = **361** passed / 3 skipped / 0 failed (+5); format clean; 0 `*.received.*` left |

## Acceptance matrix

| criterion | step(s) | verification evidence | status |
|---|---|---|---|
| C1 Hostile label text renders inert; markdown styling intact; zero golden churn | 1 | `MarkdownTests` 10/10 green (`HostileLabelsRenderInert` asserts the exported SVG contains `&lt;script&gt;alert(1)&lt;/script&gt;` and no `script` element; `PreservesStyling` pins bold/italic/sub/sup tspans unchanged); no golden re-pinned, no `*.received.*` produced | **passed** |
| C2 Unmapped ribbon survives rerender (idempotence restored) | 2 | `RenderPipelineTests.RenderTwiceUnmappedRibbonIdentical` green (pass 2 still emits the ribbon path and matches pass 1 byte-for-byte); `RenderTwiceMappedRibbonIdentical` green; the `PlotContext.cs:302-305` idempotence invariant now holds for both ribbon paths | **passed** |
| C3 Constant/leading-zero fill and size data render correctly, no NaN radii | 3 | `ContinuousAestheticScaleTests` 8/8 green — `FillRetainsLeadingZero`/`SizeRetainsLeadingZero` (0 no longer erased by the next value), `FillConstantRangeMapsToMiddleColor`/`SizeConstantRangeMapsToMidRange`, `ConstantFillLayerRenders`, `ConstantSizeLayerEmitsFiniteRadii` (`NotContain("NaN")`) | **passed** |
| C4 Month-boundary date axes render; no out-of-window ticks | 4 | `DiscreteDatesTests` 5/5 green — month, year and leap-February boundaries commit and render without throwing; `WindowedCommitEmitsNoTickOutsideTheWindow` pins `Breaks ⊆ [0, 3]` for a window whose buffer holds index 4 | **passed** |
| C5 Boxplot percentiles preserve multiplicity | 5 | `BoxplotTests` 7/7 + `SampleBufferTests` 5/5 green; `[1,1,1,10]` now yields median 1.0 (was 5.5 under dedup) and p75 3.25 (was 7.75); `DuplicateObservationsChangeTheRenderedBox` distinguishes `[1,1,1,10]` from `[1,10]` end-to-end, with a same-source control proving the id scrub leaves geometry as the only variable | **passed** |
| C6 Empty faceted source renders; invalid facet dims throw `GGNetUserException` | 6 | `EmptyFacetTests` 8/8 green — empty faceted source renders parseable SVG with no marks (with and without a legend), refilling it produces panels again, and `nrows`/`ncolumns` ≤ 0 throw at the `Facet_Wrap` surface | **passed** |
| C7 Palette exhaustion/empty throws actionable `GGNetUserException` | 7 | `PaletteValidationTests` 5/5 green — exhaustion message names the trained key count, the palette length and the required minimum; empty palettes are rejected at the surface rather than as `IndexOutOfRangeException` mid-render | **passed** |
| C8 Datetime axes order-independent; in-order output byte-identical | 8 | `DateTimePositionTests` 6/6 green; `PermutingTrainOrderYieldsIdenticalMappings` pins order-independence at the scale, `OutOfOrderSourceRendersEveryPoint` end-to-end (identical line multiset — marks differ only in document order, which follows the source); `InOrderSamplingIsUnchanged` passes both before and after the change, and no golden moved ; **post-review**: sampling moved to lazy finalization on first read after `implementation-review.md` found geoms mapping before `Commit` — `MapBeforeCommitUsesTheSampledGrid` and `TrainingAfterAReadResamples` added, and `OutOfOrderSourceRendersEveryPoint` now reads the `<g transform>` and asserts the marks span the panel | **passed** |
| C9 Transformed-scale limits act in data units; expand docs consistent (13 sites, overload tests green) | 9 | `TransformedLimitsTests` 6/6 green (log10 and sqrt limits land in scale space; identity unchanged; limited data spans the axis; break labels sane end-to-end for both `limits:` and `XLim`); 13/13 `expand` docs replaced identically, `OverloadConsistencyTests` docs-agree green, `SkillDocDriftEvals` green | **passed** |
| C10 Mixed-sign stacks render correctly; stacked x-extent equals drawn extent | 10 | `StackedBarTests` 4/4 green; 5 gallery goldens re-pinned after an eyeballed diff (listed in the step-10 row) — value-axis geometry byte-identical, grouping axis tightened as intended | **passed** |
| C11 `Polygon.Hole` honored regardless of caller winding; overlapping exteriors stay filled | 11 | `PolygonHoleTests` 4/4 green — all four source-winding combinations compose to one path, hole ring carries the opposite signed area, overlapping exteriors keep the same sign, no `fill-rule` emitted; new `GalleryTests.MapWithHole` golden pins the composed output | **passed** |
| C12 All three gates green at every step; every golden re-pin eyeballed and listed | all | All 11 steps ran the three gates green. Test count 290 → **361** passed (+71), 3 skipped, 0 failed throughout; `dotnet build -warnaserror` 0 warnings / 0 errors and `format whitespace`/`style` clean after every step. Golden re-pins: **8 total, all eyeballed and listed** — 5 in step 10 (`Bar`, `BarStacked`, `BarFlipped`, `Histogram`, `HistogramFaceted`), 3 in step 11 (`Map`, `BubbleMap`, new `MapWithHole`). Steps 1–9 churned nothing | **passed** |

## Material-delta log

| source | observed change | plan effect | decision required |
|---|---|---|---|
| user/tree | Round-2: `audit.md` broadened MF-2, added MF-11; counter-review adjudicated in `audit-challenge.md` | Plan built on reconciled chain | none |
| verification | Sandbox blocks MSBuild/vstest IPC; build needs no-server flags, test needs sandbox-off | Recorded in invariants + authority | none (approved) |
| user | Four decision gates resolved 2026-07-28 (see table) | Steps 6, 7, 9, 11 concretized | none |
| verification (step 1) | `dotnet format` also fails in-sandbox — its Roslyn build host cannot bind its named pipe (`SocketException (13)`), same class as the vstest block | Format gate joins `dotnet test` as sandbox-off; only `dotnet build` runs sandboxed (with the no-server flags) | none (within approved authority) |
| user (2026-07-28, mid-step-3) | "go all steps unless gate decision block" | Cadence flipped to **autonomous** for steps 3–11; scope and authority unchanged (still STOP on any new decision gate, forbidden churn, or unrecoverable failure) | none |
| review (2026-07-28) | `plan/q-audit/implementation-review.md` landed one must-fix: step 8's sampling ran at `Commit`, but `PlotContext.Render` shapes **before** it commits, so geoms mapped against the observed-only buffer (0, 1, 2) while the axis committed over the sampled grid (0, 30, 60). `DateTimePositionTests` missed it by reading circles' constant `cx`/`cy` instead of the wrapping `<g transform>` | **Fixed**: sampling is finalized lazily on first read (`EnsureSampled` from `Map`/`Unmap`/`Commit`), invalidated by `Train` and `Clear`. Tests corrected to read the transform and assert the marks span the panel; two new cases pin the pipeline's map-before-commit order and re-training after a read. Fail-before verified against the reviewed code | none — resolved |
| review follow-up (2026-07-28) | The datetime path had no pinned render at all, which is why the regression shipped green: its only cover was unit tests written in an order the pipeline never uses (`Commit` then `Map`) | **Coverage adjusted**: new `GalleryTests.DateTimeAxis` golden (six half-hour breaks across midnight, both date titles, marks spanning the panel) and new `PositionScaleContractTests` pinning `Map` stability across `Commit` for all six position scales — the bug's whole class, not just its instance. Both fail against the reviewed code | none — resolved |
| review (2026-07-28) | The Outcome claimed the public surface never moved; the SparkLine/`PlotBase` follow-on had invalidated that | Outcome and the non-goal now state the break explicitly | none — resolved |
| step 6 | `Components/SparkLine.razor.cs:44` also indexes `Panels[0]` unconditionally. Not reachable through the audited path (SparkLine calls `Init(false)`, so `BuildLegends` never runs) but it would throw for a faceted SparkLine over an empty source | **Moot as of 2026-07-28**: the user first ruled it refactor work rather than a point guard (SparkLine was orphaned — zero references outside its own files, and `SparkLineTooltip.RenderForeignObject` was entirely commented out), then had SparkLine removed outright in follow-on work outside this plan, along with the now-single-implementer `PlotBase`. The unguarded `Panels[0]` no longer exists | none — resolved |
| step 4 | Test-project `NodaTime` promoted to `GlobalUsings.cs` (steps 4 and 8 both need it); this made `ViewCommandTests.cs:1`'s per-file `using NodaTime;` redundant → `IDE0005` failed the style gate | Redundant per-file using removed; format gate green. Matches the C# guide's "promote a widely-used import to GlobalUsings" rule | none |
